# packages/config — общие конфиги инструментов

Базовые конфиги для JS-части монорепо: `eslint` (TypeScript strict, react-hooks, import order),
`prettier`, `tsconfig.base.json`. Пакеты и приложения расширяют их, а не копируют.

Статус: создаётся на этапе 0.
