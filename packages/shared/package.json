{
  "name": "@heatseeker/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Enum'ы, матрица прав и константы, общие для клиентов. src/generated — генерируется из Go.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "vitest run"
  },
  "devDependencies": {
    "@heatseeker/config": "workspace:*",
    "eslint": "^10.0.0",
    "typescript": "~6.0.3",
    "vitest": "^3.2.0"
  }
}
