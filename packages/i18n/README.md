# packages/i18n — словари интерфейса

i18next-ресурсы: `ru` (основной, полный), `en` (каркас). Все UI-строки mobile и web — только
через словари; ключи по модулям (`auth.*`, `materials.*`, `threads.*`, …). Серверные ошибки
приходят кодами и переводятся здесь.

Статус: создаётся на этапе 0.
