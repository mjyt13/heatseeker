# Heatseeker — открытые вопросы

Что ещё не решено, с рекомендацией и этапом, к которому нужен ответ. Решённые пункты
переносятся в [`DECISIONS.md`](DECISIONS.md).

| # | Вопрос | Рекомендация | Нужно к |
|---|---|---|---|
| 1 | Загрузка на Диск из приложения: включать ли `FEATURE_DRIVE_UPLOAD` в MVP (способ A через сервисный аккаунт доступен — SA уже Редактор, файлы попадают в его квоту 15 GB) или оставить только нативную загрузку через Drive-приложение | не в MVP; включить способом A по запросу группы | этап 1 |
| 2 | Сервер: ОС, ресурсы, доступ (docker/podman или только ssh?) — определяет compose vs systemd и включение MinIO / ClamAV / LibreOffice | узнать до этапа 5–6; код готов к обоим вариантам | этап 5 |
| 3 | Дефолтный `media_mode` группы при наличии MinIO: CACHE или IMPORT; TTL кеша и лимит | CACHE, 7 дней, 20 GB | этап 5 |
| 4 | Подписка на `MESSAGE_NEW` по умолчанию — все треды группы или только те, где участвовал / закладка | все, с mute по теме | этап 2 |
| 5 | Поведение при заполнении кеша медиа: вытеснение LRU или отказ в кешировании | LRU по `last_access_at` | этап 5 |

## Закрыто в ходе ревью плана

UI-кит (Tamagui, D4); публичное чтение L0 — выключенный переключатель (D8); навигация
обсуждений по предмету, занятия — с этапа 3 (D11); очереди на Redis + asynq (D3); бэкенд Go
(D2); хранилище MinIO (D5); Drive через сервисный аккаунт (D7); ДПО — отдельная группа (D16);
дистрибуция APK (D25); Expo для mobile и Vite для веба (D4); auth своя, трёхуровневая (D8);
`join_policy=APPROVAL` — не в первой версии (D28); сервисный аккаунт — Редактор папки, остальные
читают (D29); ИИ-сервис на Python (D30).
