# Heatseeker — открытые вопросы

Что ещё не решено, с рекомендацией и этапом, к которому нужен ответ. Решённые пункты
переносятся в [`DECISIONS.md`](DECISIONS.md).

| # | Вопрос | Рекомендация | Нужно к |
|---|---|---|---|
| 1 | Загрузка на Диск из приложения: способ A (через сервисный аккаунт — файлы в квоте SA 15 GB) / B (Google OAuth студента с ограничениями restricted scope) / не нужна в MVP — только нативно через Drive-приложение | не нужна в MVP; позже — A | этап 1 |
| 2 | Права сервисного аккаунта на общую папку: Читатель (достаточно для LINK/CACHE/IMPORT) или Редактор (для способа A) | Читатель | этап 1 |
| 3 | Сервер: ОС, ресурсы, доступ (docker/podman или только ssh?) — определяет compose vs systemd и включение MinIO / ClamAV / LibreOffice | узнать до этапа 5–6; код готов к обоим вариантам | этап 5 |
| 4 | Язык ИИ-сервиса: Python (экосистема локальных моделей, транскрибация) или Go (один язык в репо) | Python | W2-B |
| 5 | Дефолтный `media_mode` группы при наличии MinIO: CACHE или IMPORT; TTL кеша и лимит | CACHE, 7 дней, 20 GB | этап 5 |
| 6 | Подписка на `MESSAGE_NEW` по умолчанию — все треды группы или только те, где участвовал / закладка | все, с mute по теме | этап 2 |
| 7 | Нужен ли `join_policy=APPROVAL` (заявка → одобрение) в первой версии, или только OPEN / INVITE | только OPEN / INVITE; модель готова к APPROVAL | этап 0 |
| 8 | Поведение при заполнении кеша медиа: вытеснение LRU или отказ в кешировании | LRU по `last_access_at` | этап 5 |

## Закрыто в ходе ревью плана

UI-кит (Tamagui, D4); публичное чтение L0 — выключенный переключатель (D8); навигация
обсуждений по предмету, занятия — с этапа 3 (D11); очереди на Redis + asynq (D3); бэкенд Go
(D2); хранилище MinIO (D5); Drive через сервисный аккаунт (D7); ДПО — отдельная группа (D16);
дистрибуция APK (D25); Expo для mobile и Vite для веба (D4); auth своя, трёхуровневая (D8).
